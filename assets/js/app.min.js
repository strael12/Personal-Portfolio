const openCanvas=document.querySelector(".canvas");const menuCanvas=document.querySelector(".off-canvas-menu");const closeCanvas=document.querySelector(".close");const backdrop=document.querySelector(".backdrop");const nameField=document.getElementById('name');const emailField=document.getElementById('email');const subjectField=document.getElementById('subject');const textareaField=document.getElementById('textarea');const sendButton=document.getElementById('send');const mainAdjust=document.querySelector('.ajuste');sendButton.setAttribute('disabled','disabled');mainAdjust.addEventListener('input',(e)=>{Checkform(nameField,emailField,subjectField,textareaField)});closeCanvas.addEventListener("click",toogleCanvas);openCanvas.addEventListener("click",toogleCanvas);